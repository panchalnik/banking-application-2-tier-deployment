name: Test Connection for pipeline

on:
  workflow_call:
    

jobs:
  working-job:
    runs-on: ubuntu-latest

    steps:
      - name: Echo Message
        run: echo "GitHub Actions is working successfully ➡️"