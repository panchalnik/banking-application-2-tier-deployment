name: Smoke Testing for Infrs and Networking

on:
  workflow_call:
    

jobs:
  working-job:
    runs-on: ubuntu-latest

    steps:
      - name: Echo Message
        run: echo "Smoke Testing is done successfully ➡️"